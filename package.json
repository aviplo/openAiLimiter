{
  "name": "open-ai-limit",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev:local --prefix backend\"",
    "init": "npm install && cd frontend && npm install && cd .. && cd backend && npm install && cd .. && npm run dev"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
